import type { SectionIds } from '@/types/resumeData';

/**
 * Id’s of all sections in the default order.
 */
const possibleSectionIds: SectionIds = [
  'personal',
  'links',
  'skills',
  'experience',
  'projects',
  'education',
  'certifications',
];

export default possibleSectionIds;
